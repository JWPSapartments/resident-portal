// Single source of truth for brand identity. Change it here and every
// page (sidebar, topbar, login, review, application, tab title) follows.
export const BRAND = {
  name: 'JWPS Apartments',
  // Optional line under the brand mark (e.g. location or slogan).
  // Leave empty to hide it.
  tagline: '',
}
